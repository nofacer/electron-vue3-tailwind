{
  "name": "electron-vue3-tailwind",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "prebuild": "vue-tsc --noEmit && vite build",
    "build": "tsc",
    "preview": "vite preview",
    "electron-run": "electron ./dist",
    "preelectron-run": "vue-tsc --noEmit && vite build && tsc"
  },
  "dependencies": {
    "@heroicons/vue": "^1.0.5",
    "vue": "^3.2.25",
    "vue-router": "^4.0.12"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^2.0.0",
    "autoprefixer": "^10.4.0",
    "electron": "^16.0.5",
    "postcss": "^8.4.5",
    "tailwindcss": "^3.0.7",
    "typescript": "^4.4.4",
    "vite": "^2.7.2",
    "vue-tsc": "^0.29.8"
  }
}
